{% for id, quantity in cart %}
<div>
  {% for i in range(1, quantity) %}
    <a title="Remove this item" href="{# path('cart_remove', {'id': id}) #}">
     <h1>Product Id in your basket: {{ id }}</h1></br></h1>
    </a>
  {% endfor %}
</div>
{% else %}
	nothing yet in your shopping cart.
{% endfor %}